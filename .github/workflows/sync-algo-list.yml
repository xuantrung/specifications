name: Sync Algorithm List

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  update-json:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download JSON file
        run: |
          # The key change is the path: supplemental-ui/
          mkdir -p supplemental-ui/softbinging-alg-list
          curl -s https://raw.githubusercontent.com/c2pa-org/softbinding-algorithm-list/refs/heads/main/softbinding-algorithm-list.json -o supplemental-ui/softbinging-alg-list/index.json

      - name: Commit and push if changed
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add supplemental-ui/softbinging-alg-list/index.json
          git diff --quiet && git diff --staged --quiet || (git commit -m "Update softbinding algorithm list" && git push)
